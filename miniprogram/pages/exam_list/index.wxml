<view>
	<view wx:for="{{tiList}}" bindtap="goExam" data-item="{{item}}">
		<van-cell value="" size="large">
			<view slot="title">
				<view class="van-cell-text text-base font-medium">{{item.name}}</view>
			</view>
		</van-cell>
	</view>
</view>